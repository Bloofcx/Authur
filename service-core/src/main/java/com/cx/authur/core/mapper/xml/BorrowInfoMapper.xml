<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cx.srb.core.mapper.BorrowInfoMapper">

    <select id="getList" resultType="com.cx.authur.core.pojo.entity.BorrowInfo">
        SELECT bi.*,user_info.name,user_info.`mobile`
        FROM borrow_info AS bi LEFT JOIN user_info ON bi.`user_id` = user_info.`id`
        WHERE bi.is_deleted = 0;
    </select>
</mapper>
